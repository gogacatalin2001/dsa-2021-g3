#include <stdio.h>
#include <stdlib.h>

/*int truckOnRoad(int key, ListT *listPtr, NodeT *p)//does not checks if(there are other trucks on the same road
{
    p=createSLLNode(key);
    //insert at front or rear, it does not matter, since they can overtake each other
    if(insertAtFront(listPtr,p)==1)
        return 1;
    return 0;
}
int selectedTruckIntoG(int key, ListT *listPtr1, NodeT *p, ListT *listPtr2)//does not checks if(there are other trucks are the same
///allows to change nodes between lists, insetring it at the front of the other list
{
    //delete truck from road, adn link it to the LIFO of the garage
    //insert at front or rear, it does not matter, since they can overtake each other
    if(insertAtFront(listPtr1,p)==1)
        return 1;
    return 0;
}*/
